# 4.0 总介

```text
为了让RISC-V核心支持调试, 要修改一些内容,而我们让这些修改保持在最低限度。
增加了一个特殊的执行模式(调试模式)和一些额外的csr(0x7a0 - 0x7bF)//0x7a0 可以被 M-mode 访问
    // 0x7b0 不可被 M-mode 访问
增加了 DM 和 DTM
为了符合这个规范，实现必须实现本节中没有明确列出的 "可选的所有内容"
```
